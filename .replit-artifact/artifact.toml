kind = "web"
previewPath = "/"
title = "AI Revenue System"
version = "1.0.0"
id = "artifacts/ai-revenue-system"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/ai-revenue-system run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ai-revenue-system", "run", "build" ]
publicDir = "artifacts/ai-revenue-system/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
